#include <iostream>
#include <string>
using namespace std;

void rever(const string& str) {
    for (int i = str.length() - 1; i >= 0; --i) {
        cout << str[i];
    }
}

int main()
{
    string ab = "Testing";
    rever(ab);
    
    return 0;
}
